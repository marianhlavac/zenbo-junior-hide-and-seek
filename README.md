# Zenbo Junior Hide And Seek

## Installing

Use **pipenv**, or navigate to `pyzenbo` folder and install the SDK using 

```
$ python3 setup.py install
```

## Running

Run hide and seek using:

```
$ python3 -m hideandseek [arguments]
```